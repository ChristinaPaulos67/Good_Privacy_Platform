/* Flask-ready API adapter. Replace endpoint paths with your deployed blueprint as needed. */
window.AddisAPI = { baseUrl: '', csrfToken: null, async request(path, options = {}) { const headers = {'Content-Type':'application/json', ...(options.headers || {})}; if (this.csrfToken) headers['X-CSRFToken'] = this.csrfToken; const response = await fetch(`${this.baseUrl}${path}`, {credentials:'include', ...options, headers}); if (!response.ok) throw new Error(`Request failed: ${response.status}`); return response.status === 204 ? null : response.json(); }, login(email, password) { return this.request('/api/auth/login', {method:'POST', body:JSON.stringify({email,password})}); }, logout() { return this.request('/api/auth/logout', {method:'POST'}); }, projects() { return this.request('/api/projects'); } };
